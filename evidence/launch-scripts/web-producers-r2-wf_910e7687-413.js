export const meta = {
  name: 'web-producers-r2',
  description: 'Sealed producers round 2: R5 theory-path grammar + R2 proof-script west pane assembly (Fable), then both-sides audit',
  phases: [
    { title: 'Implement', detail: 'sealed R5 (URL/path grammar) then R2 (west pane assembly) — Fable', model: 'fable' },
    { title: 'Audit', detail: 'both-sides similarity audit of the round-2 delta' },
  ],
}

const IMPL = `You are a SEALED implementer under the barrier protocol at /home/kamilner/tamarin-cleanroom/PROTOCOL.md — read it first and comply strictly.
ACCESS RULES: You must NOT read any file under /home/kamilner/tamarin-rs/crates/, and NOT read any Haskell source of tamarin-prover anywhere on disk (notably /home/kamilner/tamarin-rs/tamarin-prover/{lib,src}/ and /home/kamilner/tamarin-rs/tamarin-prover-testing/{lib,src}/). Do NOT read /home/kamilner/tamarin-cleanroom/INTEGRATION_REPORT.md or TAIL_ELIMINATION_SCOPE.md, and do NOT read any AUDIT.md anywhere in the cleanroom. No web access (the local HS oracle server is not web access). You MAY read .spthy example files under /home/kamilner/tamarin-rs/tamarin-prover/examples/, run the sanctioned oracle harnesses, and read the captured corpus /home/kamilner/tamarin-rs/scripts/.web_hs_cache. No git commits.
DISCIPLINE: log every oracle interaction in workspace QUERIES.log; record behavioral facts in BEHAVIOR.md with probe provenance; comments describe current behavior only. Oracle scripts carry OOM guards — preserve them; live HS servers only on ports 3100-3199; STOP every server you start before returning. Return a PLAIN-TEXT summary.

Workspace: /home/kamilner/tamarin-cleanroom/weblayer/ (read/write freely inside it, EXCEPT AUDIT.md). The producers cluster lives at weblayer/producers/ — READ FIRST: producers/SPEC.md, producers/README.md, producers/interface/{fragment_inputs.rs,required_api.md}, then the round-1 sealed state: producers/workspace/BEHAVIOR.md + QUERIES.log and the crate producers-clean (R1 — the shared HTML skin, escape/postprocess/envelopes, center section fragments — is DONE: 14 tests, 324/324 corpus sweep; R2–R5 are stubs). You may reuse everything R1 established.

THE TASK — round 2 = sub-targets R5 then R2 (R5 first; R2 consumes its links).

R5 — the theory-path grammar (pure, HTML-free): the URL wildcard segment ⇄ structured path mapping used by every fragment link (hrefs in the captures) and by the server's routing. Characterize it from BOTH observable sides: (a) RENDER: harvest the href vocabulary across the whole capture corpus (overview/proved trees included — extract_fragments.py helps) and pin segment shapes, separators, percent-encoding, the underscore-prefix quirks, numeric segment forms; (b) PARSE: probe the LIVE oracle with constructed URLs — accepted paths return pages, rejected ones return the error/404/redirect surface — to pin acceptance, normalization (does a decoded/re-encoded variant reach the same resource?), and edge cases (empty segments, doubled separators, stray percent sequences, case). Implement the R5 entry points in producers-clean per required_api.md, with parse⇄render round-trip tests plus byte tests against harvested hrefs, and live-probe replays for parse decisions. Record genuinely unreachable corners as UNOBSERVABLE with the probes that failed.

R2 — the proof-script WEST pane assembly (the 'overview' capture family, 473 captures): 'theory NAME begin' … 'end' — item links (message/rules/tactic/sources with labels + annotations), the add-lemma link, per-lemma declaration lines (name + attributes + quantifier + opaque formula text), proof display, edit/delete/add links. This round is the ASSEMBLY: the pane's frame, ordering, link construction (via R5), and line composition, reusing R1's skin (escape/postprocess/envelope). Lemma FORMULA text and proof-tree node text are opaque pre-computed inputs (slice them from captures). The fresh no-prove pane state is pure render; use proved-state captures where they only add opaque content. Acceptance: a corpus sweep over ALL overview captures in the 81 manifests asserting slice-and-re-render byte-identity of the pane (envelope included), same methodology as R1's 324/324 gate, plus live-probe replays on at least two never-captured theories; include a mutation check proving the sweep is not vacuous.

All cargo tests green, zero warnings, std-only. Do NOT edit anything outside /home/kamilner/tamarin-cleanroom/weblayer/.`

const AUDIT = `You are a BOTH-SIDES similarity auditor for the tamarin-rs sealed relicensing campaign (exempt from barrier access rules; you may read everything). Audit ONLY this round's delta: /home/kamilner/tamarin-cleanroom is a git repo whose HEAD (b4810fe) contains producers round 1 — git -C /home/kamilner/tamarin-cleanroom status/diff restricted to weblayer/ shows the round-2 delta (R5 path grammar + R2 west-pane assembly in producers/workspace/).
Audit against the upstream Haskell web layer in /home/kamilner/tamarin-rs/tamarin-prover/src/Web/ — Types.hs (the path/URL types and their parse/render instances) and Theory.hs (the theory-index / proof-script pane producers). The path grammar is small and behavior-dense: distinguish carefully between MERGER (any correct implementation must accept/emit these exact strings — pinned by probes/captures) and copied EXPRESSION (upstream's decomposition into helpers, identifier constellations, non-observable internal representations, comment lineage). Cross-check that every grammar rule and pane-assembly rule in BEHAVIOR.md traces to a logged probe or harvested capture, not to the source. Also confirm the PH-1 scaffold rewrite (SPEC.md/README.md no longer cite upstream file paths) landed and nothing re-introduced upstream citations into sealed-readable files.
Append a producers round-2 section to /home/kamilner/tamarin-cleanroom/weblayer/AUDIT.md. Violations get BEHAVIORAL redo instructions (never a patch). End with exactly one line: VERDICT: pass  — or —  VERDICT: fail`

const impl = await agent(IMPL, { label: 'seal:producers-r2', phase: 'Implement', model: 'fable' })
const audit = await agent(AUDIT, { label: 'audit:producers-r2', phase: 'Audit', model: 'opus' })
return { impl: (impl || '').slice(0, 3000), audit: (audit || '').slice(0, 2500), passed: /VERDICT:\s*pass/i.test(audit || '') }
export const meta = {
  name: 'producers-integration',
  description: 'Open-side joint web-producers integration: vendor producers-clean R1-R5, adapter-route the producer surface, gate (web_parity + suites + live probes), attempt deletion of the four producer files and the dispatch shells',
  phases: [{ title: 'Integrate', detail: 'vendor + adapters + gates + deletion attempt (Opus)' }],
}

const GATE = `You are the OPEN-SIDE integrator for the tamarin-rs sealed relicensing campaign, working in /home/kamilner/tamarin-rs. This is the campaign's most consequential integration: the sealed web PRODUCER surface (R1-R5) is complete, and your job is to wire it in, gate it, and attempt the deletions that erase the remaining web-cluster authors.
READ FIRST: /home/kamilner/tamarin-cleanroom/PROTOCOL.md ("Full-corpus gates"); the weblayer/A-unit and producers sections of /home/kamilner/tamarin-cleanroom/INTEGRATION_REPORT.md (the A-adoption history: StateOps/origin-aware shell verified in A7, the round-9 citation-topology finding); /home/kamilner/tamarin-cleanroom/weblayer/producers/{SPEC.md,README.md,interface/required_api.md} and the sealed crate producers/workspace/producers-clean/ with its BEHAVIOR.md (the behavioral spec, sections on skin/postprocess/envelopes, path grammar, west pane incl. the ≤69 escaped-width layout law, proof-tree grammar, welcome page).
WHAT EXISTS: producers-clean implements — R1 shared HTML skin (escape, <br/>/&nbsp; postprocess, {html,title}/{redirect}/{alert} envelopes) + the four main/* center sections; R5 URL⇄path grammar; R2 west proof-script pane assembly; R3 proof-tree/method HTML; R4 welcome/index + housekeeping bodies. All byte-gated sealed-side (478/478 pane sweeps, live replays). The already-vendored web_clean carries the A-side dispatch/state machine/forms/shells.
RULES: vendor sealed sources mechanically (documented path-fix recipe; headerless; reverse-transform byte-identity; a clean file acquiring a GPL header is a provenance violation — stop and report). Adapters are open-side and translate live values (closed theory data, proof trees, lemma/formula text, index rows) into the producers-clean input model — never transplant render logic. No git commits. PATH needs /home/linuxbrew/.linuxbrew/bin. Live HS servers only on ports 3100-3199; stop everything you start; preserve OOM guards.
STEPS, in priority order (partial completion with precise keep-and-report beats overreach):
1. Vendor producers-clean into crates/tamarin-server/ (e.g. src/producers/ or merged into the web_clean module tree — your choice, document it). Build + full workspace test suite green before any routing changes.
2. Write the adapters and route the PRODUCER surfaces through the clean crate, one at a time, gating each: (a) the four main/* center sections + envelopes (R1); (b) theory-path rendering/parsing at the link boundary (R5) — note handlers/path_parse.rs is the parse side; (c) the west proof-script pane (R2) with proof lines through R3; (d) the welcome/index page + housekeeping (R4). After each routing step: cargo test suite for tamarin-server + bash scripts/web_parity.sh (full crawl — no new divergences vs the cache) + a live HS side-by-side spot probe on at least 2 theories (serve both, byte-compare the routed responses).
3. DELETIONS — for each of handlers/theory_html.rs, handlers/proof_tree.rs, handlers/root.rs, handlers/path_parse.rs: delete it once every caller routes through the clean crate and all gates are green. Keep-and-report any file a gate blocks, with byte-precision blockers.
4. DISPATCH SHELLS: with the producers routed, attempt the standing A-adoption — route dispatch through web_clean's StateOps/shell machinery so the ported theory.rs/state.rs dispatch code can shrink or delete. The A7 concurrency contract (snapshot→compute→commit, non-blocking reads) was live-verified; the origin-aware shell landed in A6. The three no-clean-home routes (proof-step progressive UI, graph = dot -Tsvg, unload) STAY ported — factor them so they survive the shell deletion. This step has historically blocked; if it blocks again, characterize precisely and keep — the producer deletions alone are a major win.
5. Headers: python3 scripts/gen_license_headers.py (regenerate) + --check. Report the header-count delta AND the author-level delta: which authors' citations disappeared entirely from the repo, which lost files (especially Kanakanajm, YannColomb, Esslingen-Security-Privacy, cascremers, arcz, felixlinker, and the proof_tree.rs 18-author list).
6. Final full gates: cargo build --release; full workspace cargo test; scripts/web_parity.sh; JOBS=6 bash scripts/wf_gate.sh (no regression vs scripts/wf_gate_round7.tsv); RESULTS_TSV=scripts/pretty_gate_check.tsv JOBS=6 bash scripts/pretty_gate.sh (403/16 baseline held).
Append a dated producers-integration section to /home/kamilner/tamarin-cleanroom/INTEGRATION_REPORT.md. Return a plain-text summary: what routed, what deleted, what kept (with blockers), all gate numbers, and the author-erasure arithmetic.`

const gate = await agent(GATE, { label: 'integrate:producers', phase: 'Integrate', model: 'opus' })
return { gate: (gate || '').slice(0, 4500) }
export const meta = {
  name: 'pretty-final-integration',
  description: 'Open-side pretty endgame: re-sync R4, route rules + formulas/lemmas/restrictions + macros through clean, dead-code deletion analysis in pretty_theory.rs/pretty_formula.rs, header regeneration + author delta',
  phases: [{ title: 'Integrate', detail: 'full-echo routing + deletion analysis + author arithmetic (Opus)' }],
}

const GATE = `You are the OPEN-SIDE integrator for the tamarin-rs sealed relicensing campaign, working in /home/kamilner/tamarin-rs. This is the pretty unit's ENDGAME integration: the sealed crate (R1 term+signature, R2 rules+macros, R3 formulas/lemmas/restrictions, R4 blocker fixes) is complete and audited; the signature block already routes clean (adopted in the round-2 integration). Your job: route the remaining echo surfaces, then perform the deletion analysis that shrinks the ported render files' headers.
READ FIRST: the pretty sections of /home/kamilner/tamarin-cleanroom/INTEGRATION_REPORT.md (round-1: vendoring recipe + pretty_clean_adapt.rs; round-2: signature swap adopted, rule route reverted with 3 witnesses now FIXED in R4 — process= attribute emitted, >)-case was stale, stack overflow fixed with an iterative Doc engine), pretty/SPEC.md, and PROTOCOL.md.
RULES: mechanical re-sync (crate::->super:: recipe; headerless; reverse-transform byte-identity). Adapters translate values only. No git commits. PATH needs /home/linuxbrew/.linuxbrew/bin. Deleting or modifying HEADERED ported code is allowed and expected in step 4 — that is open-side work — but NEVER move ported logic into headerless files. Preserve OOM guards.
STEPS:
1. Re-sync the round-3+4 pretty-clean sources into crates/tamarin-theory/src/pretty_clean/ (R3 added formula.rs/lemma.rs and extended ast.rs; R4 changed rule.rs and doc.rs — the iterative engine). Extend pretty_clean_adapt.rs for the new inputs (formulas, lemma/restriction wrappers, guarded text as opaque input, proof-script tails). Build + full workspace tests green.
2. ROUTE, one surface at a time, each gated by RESULTS_TSV=scripts/pretty_gate_final_<n>.tsv JOBS=6 bash scripts/pretty_gate.sh expecting EXACTLY 403 MATCH / 16 DIFF (revert the offending route on ANY new DIFF, keep the rest, record witnesses): (a) rule rendering (retry — R4 closed all three witnesses; verify ct.spthy, ake/dh/*, and the four deep files that previously crashed all MATCH); (b) restriction + lemma rendering (incl. the guarded-formula comment frame with the guarded text supplied opaque from the ported transform); (c) the macros block; (d) whatever echo assembly glue is now expressible cleanly (theory frame ordering) — only if byte-green.
3. Also route the WEB signature pane: round-2 kept ported render_signature only for web_signature_block — route it through the clean crate too and gate with bash scripts/web_parity.sh; if green, the last render_signature caller is gone.
4. DELETION ANALYSIS — the payoff: with the routes live, enumerate every function in crates/tamarin-theory/src/pretty_theory.rs and pretty_formula.rs that is now DEAD (no remaining caller; the compiler + cargo-udeps-style reasoning is your friend — dead_code warnings after removing pub, call-graph grep). DELETE the dead render functions (solver-entangled surfaces — pretty_system.rs, proof rendering, wf assembly glue, web panes that still call ported code — STAY). Rebuild + full gates after each deletion batch. Then regenerate headers: python3 scripts/gen_license_headers.py && python3 scripts/gen_license_headers.py --check. Report the header delta AND the per-author delta on pretty_theory.rs and pretty_formula.rs (the before lists are in the current headers — name who dropped, who remains and via which surviving cited ranges).
5. FULL GATES: cargo build --release; cargo test --workspace; pretty_gate (403/16); JOBS=6 bash scripts/wf_gate.sh (419/0 vs scripts/wf_gate_round7.tsv); bash scripts/web_parity.sh; console_split_parity suite explicitly. Keep-and-report anything blocked with byte witnesses.
Append a dated pretty final-integration section to /home/kamilner/tamarin-cleanroom/INTEGRATION_REPORT.md. Return a plain-text summary: surfaces routed, functions deleted (counts + LOC), header/author arithmetic, gate numbers, residual blockers.`

const gate = await agent(GATE, { label: 'integrate:pretty-final', phase: 'Integrate', model: 'opus' })
return { gate: (gate || '').slice(0, 4500) }
# QUERIES.log — every oracle interaction, with purpose

## Corpus reads (oracle/captured_responses/, captured OUTPUT)
- Enumerated 81 manifest files; each is {base,lemmas,log,capped,manifest}.
  manifest = {url -> {kind,status,body}}. Purpose: discover payload families.
- Surveyed URL patterns: found graph families
  `/thy/trace/#/interactive-graph-def/proof/...` (kind=dot, THE graph payload)
  and `/thy/trace/#/intdot/proof/...` (kind=html wrapper embedding <dot-graph-viz>).
- Dumped small DOT payloads (sessions_injective case_1; can_run_v/_) to learn
  node/edge/cluster/abbreviation syntax.
- Per-file survey: simple vs compact(cluster) header; abbreviation-table presence.

## Live server probes (hs_server.sh)
(logged below as executed)

## Live server probes (executed)
- Started black-box HS binary via corrected wrapper (scratchpad serve.sh: same
  binary as hs_server.sh, but `--port=` form; provided script used space-separated
  `--port N` which this binary parses as a positional WORKDIR -> "directory '3099'
  does not exist"). Served csf12/NAXOS_eCK.spthy on :3099.
  Purpose: discover graph endpoints, option toggles, DOT-vs-JSON backend.
- GET / , /thy/trace/1/overview/help : found theory index number replaces `#`;
  route families confirmed (main/*, interactive-graph-def, intdot).
- GET /thy/trace/1/interactive-graph-def/proof/<lemma>[/steps] : returns kind=dot
  (text/plain). Root state => empty digraph. Purpose: confirm live == corpus format.
- GET /thy/trace/1/interactive-graph-def/cases/raw/1/1 : non-empty case graph with
  abbreviation (EX1/EX2/H2). Purpose: get a live abbreviated graph.
- Probed query params (?simplification=,?abbreviate=,?level=,?compress=,?simplify=,
  ?s=, ...) on the graph endpoint: ALL byte-identical to no-param. => graph options
  are NOT URL-controlled; no per-request simplification-level knob is exposed.
- `tamarin-prover interactive --help`: graph-relevant flags = --image-format[PNG|SVG],
  --with-dot[=FILE], --with-json[=FILE]. => JSON graph payload is a SEPARATE backend
  (--with-json rendering tool); the captured corpus is the DOT backend. No CLI
  simplification-LEVEL flag exists.

## Corpus validation (reads of captured OUTPUT only)
- Extracted 6 representative DOT payloads into graph-clean/tests/fixtures/
  (empty; simple+ports; simple+abbrev; compact+2clusters; compact+abbrev;
  multi-row abbrev legend). Purpose: byte-parity fixtures.
- Bulk round-trip: parsed every captured DOT payload into the crate model and
  re-serialized. Samples run: 1823 (random across all 81 theories), 1845 (all
  1559 clustered + 400 largest), and finally ALL 14705 payloads.
  Result: 14705/14705 byte-exact. Purpose: prove the payload-format spec +
  serializer are general, not fixture-overfit.

## Session 2 — abbreviation SELECTION rule (§5c), captured-output mining + live probes

### Corpus reads (captured OUTPUT: oracle/dot_corpus/, oracle/captured_responses/)
- Enumerated manifest URL families: main/proof (json, 14705) pairs 1:1 with
  interactive-graph-def (dot, 14705); the main/proof json html carries the full
  UNABBREVIATED sequent (Constraint system: formulas/goals/solved+unsolved
  constraints), while the dot carries abbreviated terms + legend. Mined these
  pairs at scale for a labeled abbreviate/not dataset.
- Parsed all 97 538 legend expansions across 11 564 graphs: min fully-expanded
  rendered length = 10 (0 below); 0 legend entry is a top-level tuple.
- Built AST parser for tamarin printed terms (fact-arg extraction, name
  expansion, structural subterm occurrence). Ran per-graph labeled evaluation
  over up to 5 725 graphs / 156 602 candidates. Findings: len>=10 & occ>=2 &
  not-tuple is necessary with 0 counterexamples among drawn->=2 terms; all
  "missed" abbreviations have graph-occ 1 (recovered from the sequent);
  sufficiency exceptions are 93% AC/DH-operator graphs (occurrence over the
  normalised term), rest are expand-before-count artifacts.
- Confirmed abbreviation universe > drawn nodes: SI3='sign(~y, k)' is a legend
  entry drawn NOWHERE in its graph (only in the sequent).

### Live server probes (hs_server binary, --port= form; crafted theories)
Served four one-rule theories whose single graph node carries controlled terms;
solved the exists-trace lemma via GET /main/method/<lemma>/1 (returns a redirect
to a new theory index), then GET interactive-graph-def/proof/<lemma>/_ .
- LenTest: Keep(~x,'12345678','12345678','1234567','1234567') ->
  '12345678'(10 chars,x2) abbreviated as `2`; '1234567'(9,x2) inline.
  => length threshold is exactly 10.
- OccTest: 'ABCDEFGH'(10,x1) & 'onceonly12'(12,x1) inline; 'twicelong1'(12,x2)
  => TW1. LongOnce: a 42-char constant (x1) inline; 'shared1234'(x2) => SH1.
  => occ>=2 is a hard gate independent of length.
- TupTest: <'aa','bb','cc'>(18,x2) inline; h('longarg123')(x2) => H1, with
  LO1='longarg123', H1='h(LO1)'. => tuples never abbreviated; bottom-up nesting;
  prefix rule (lo->LO, h->H) re-confirmed live.
- Servers stopped after probing. Note: pure-numeric constant got empty prefix =>
  bare-number name ('12345678' -> "2"); empty-prefix start index not pinned.

## Session 3 — GENERATION (SPEC_ROUND3): rendering, shapes, id allocation, content, levels

### Corpus reads (captured OUTPUT: oracle/dot_corpus/ 12022 payloads, captured_responses/)
- Shape census over all 12022 dot + all 81 manifests: shapes = record(160409),
  ellipse(188270), plain(11564). NO trapezium/invtrapezium anywhere in the crawl.
- Node-id/PORT allocation (mine_ids.py): verified over ALL 12022 graphs that ids
  are one global counter in emission order (== file order), a record consuming one
  id per cell (premises->info->conclusions) THEN one node id; other kinds one id.
  12022/12022 byte-consistent. (Re-verified in Rust: tests/alloc_corpus.rs,
  12022/12022 via the crate's own NodeIdAllocator.)
- Record group structure (mine_groups.py): 100% of 160409 records carry an info
  cell (#t : Rule); groups = present-of [premises,info,conclusions], EMPTY premise
  or conclusion group dropped. dist: 3grp 153854 (prem|info|concl), 2grp info@0
  2147 (info|concl, e.g. Fresh), 2grp info@1 4406 (prem|info), 1grp 2.
- Record-cell WRAP mining (mine_wrap.py/mine_indent.py/mine_width.py): 188192
  wrapped cells. Continuation indent = &nbsp; run equal to the column of the
  broken group's FIRST element (after "( " for a fact, after "<" for a tuple,
  after "[" for an action list) — verified on many cells. Segments joined by \l,
  trailing \l after the last line. Break-point chars: , ) space > ] . First-line
  length ranges 5..87 (NOT a flat width) => the break DECISION is the pretty-
  printer's width/group internal (GAP); the FORMAT+indent are exact.
- Fact-vs-function spacing: facts pad `Name( a, b )` / `Name( )`; functions do
  not `f(a, b)`; tuples `<a, b>`; info cell `#t : Rule[acts]` or bare `#t : Rule`.
- Content vocabulary census (mine_content.py): edge attr signatures = 11 fixed
  styles (red-dashed !KU deduction; bold w10 [+gray50] structural; gray30 message;
  blue3-dashed / black-dashed temporal; orangered2; invis; purple/green/darkorange
  goal edges). Ellipse colors: none / gray (!KU) / darkblue (action). Ellipse
  kinds: !KU(m)@#t, Action(..)@#t, compressed `#t : rule[..]`.
- Query-param census: grep of manifests found the UI menu "Graph simplification
  off/L1/L2/L3" (lvl0..lvl3 toggles). Captured graph URLs carry NO query params.

### Live server probes (corrected wrapper: scratchpad/serve.sh, same black-box
### binary as oracle/hs_server.sh but --port= form; ports 3200-3201)
- Read /static/js/tamarin-prover-ui.js (served BY the black-box): the graph URL
  query params are cookie-driven — `simplification=N` (default 2, always sent);
  at level 0 additionally `uncompact=` + `uncompress=`; `unabbreviate=` when the
  abbreviate cookie is off; `no-auto-sources=`; `clustering=true`. This is the
  census SPEC item 5 refers to.
- NAXOS_eCK on :3200; NSLPK3 on :3201. Discovered theory index via GET /; probed
  case graphs GET interactive-graph-def/cases/{raw,refined}/i/j (available without
  solving).
- Simplification-level diffs on NAXOS refined/1/2 and largest NSLPK3 case:
  default == simplification=1 == 2 == 3 (byte-identical on every probed graph;
  L1/L2/L3 distinction UNobserved => GAP). Level-0 (uncompact+uncompress) EXPANDS
  the graph: Fresh sources + intruder rules become full records; !KU ellipses and
  isend/coerce ellipses re-expand. unabbreviate inlines terms and drops the legend
  sink-block. Isolated uncompact vs uncompress each partially expand.
- SHAPE hunt: invtrapezium `(#i, N)` observed live (a `(#nodevar, premiseIndex)`
  placeholder for an unresolved node referenced by an open premise; fed by the
  conclusion that must satisfy it) — in default AND uncompressed case graphs of
  BOTH NAXOS and NSLPK3. Plain `trapezium` NOT observed in any probe (all case
  graphs, compressed+uncompressed, 2 theories) nor the corpus — recorded as
  unobserved-but-spec-named. Walked eCK_key_secrecy proof steps (method/1) — small
  ellipse-only states, no new shapes.
- Saved fixtures: nsl_invtrap.dot, invtrap_compressed.dot (=NAXOS default),
  invtrap_raw.dot (=level-0 raw). Servers stopped after probing.

## Session 4 — close L1/L2/L3 gap + re-attempt wrap DECISION (session-state lens)

### Channel investigation (served JS + response headers, all HTTP-observed OUTPUT)
- Fetched /static/js/tamarin-prover-ui.js from the black-box binary. The graph
  URL params are built from CLIENT-SIDE cookies (jquery-cookie): handleJson (lines
  78–100) appends `simplification=<cookie>` always, plus `uncompact=`+`uncompress=`
  ONLY when the cookie==0, plus `unabbreviate=`/`no-auto-sources=` per their
  cookies. Level-button loop is `for i<4` (off/L1/L2/L3), default cookie=2. No
  AJAX/POST sets a graph preference server-side; the POSTs are lemma editing only.
- `curl -i` on root, case-graph, main/cases json, and a proof-method request:
  **no Set-Cookie on ANY response** ⇒ no server-side session state; the URL query
  param is the sole channel. (This is what the follow-up's "session-state lens"
  warned to check; the channel is verified to be the URL param, not a cookie.)

### Live level probes (serve.sh wrapper, --port= form; ports 3200–3203)
- NSLPK3 :3200, richest source-case graph `refined/6/5` (17 nodes, 5 668 B). Matrix
  over `simplification` ∈ {0,1,2,3} × {URL param, cookie `-b`, both}: ALL byte-
  identical to baseline (one md5). Adding `uncompact=&uncompress=` (the real level-0
  flags) DID change it (7 792 B) and was identical with/without a cookie ⇒ URL param
  honored, change verified before diffing. Level number ∈ {0,1,2,3,4,5,10,99}
  crossed with 8 flag combos: within every combo, L1/L2/L3 collapse to 1 md5;
  `flags_L0`…`flags_L99` all one md5. Individual flag effects distinct (uncompact
  6 496, uncompress 6 326, unabbreviate 4 695, clustering 6 388 B).
- NAXOS_eCK :3201 case `refined/4/1` (2 912 B): L0=L1=L2=L3 identical, L0+flags
  differs. Reproduces the negative on a 2nd theory.
- eurosp19-eccDAA analysed :3203 (DH/bilinear, deep). Big proof-state graph
  `proof/functional_correctness/_` = **39 nodes, 27 361 B**. All level probes
  (url/cookie/both, {0,1,2,3,4,10}) → 1 md5; no Set-Cookie. Flag effects measured:
  uncompact 30 880 B (records richer, still 39), uncompress **74 nodes** 31 407 B,
  unabbreviate 38 nodes/36 690 B/0 legend rows, uncompact+uncompress==level0 (74
  nodes). ⇒ CONCLUSION: no L1/L2/L3 distinction; the level number is inert; "off"
  (L0) = the UI adding two flags. GAP closed (rigorous negative + mechanism).

### Wrap-DECISION probes (crafted single-node theories; autoprove + graph fetch)
- Route discovery: autoprove via GET /thy/trace/1/autoprove/idfs/0/False/proof/<L>
  (returns redirect to a new theory index); proof-state graph at
  interactive-graph-def/proof/<L>/_ . Confirmed the served JS appends graph params
  at line 922 (dotsrc + "?" + query_string) with no width/wrap parameter; layout
  east-size/west-size cookies are client-only (never sent) ⇒ wrap has NO channel;
  it is a fixed server-side pretty-printer, re-attacked by input control.
- Two sweeps solved live (ports 3220–3236, driver.py/gen_sweep.py):
  (a) `Out(<'a01',…,'aN'>)` N=1..29 — pure fill of 5-col elements;
  (b) `Out(<'aa…a'(P), 'y'>)` P=60..95 — one-column flat-width sweep.
  Findings: single-line iff flat width ≤ **87**; 88 breaks. First line packs 11
  elements + trailing ", " (ends col 83); continuation packs 12 (fsep lookahead).
  Closing `)` peels to col 0; tuple `>` peels to the `<` column; long unbreakable
  atoms overflow verbatim.
- Functor-length invariance: `Ba`/`Out`/`Fact12`/`Factabcdef` (len 2/3/6/10) — the
  flat-fit boundary is ALWAYS flat width 87 fits / 88 breaks ⇒ W=87 is ABSOLUTE
  from column 0, functor name included. Corpus cross-check: 99.2 % of 190 044
  physical lines have fact-content ≤ 87 (residual = unbreakable atoms > 87).
- Captured raw fixtures (fixtures/wrap_fixtures.json) for the crate tests. Servers
  stopped after probing. Implemented FILL_WIDTH=87 / fits_one_line / paragraph_fill
  in graph-clean/src/render.rs with tests reproducing these probes.

## Session 5 — complete generate(): clusters, wrap wiring + residuals, node kinds, pre-rendered entry

### Corpus reads (captured OUTPUT: oracle/dot_corpus/ 12022, captured_responses/)
- CLUSTER structure census (analyze_clusters.py/analyze_clusters2.py over 1457
  cluster files): top-level statement order is ALWAYS [free nodes][clusters]
  [edges][rankblock][edges] — 0 files with a top-level node after a cluster, 0
  clusters after an edge. Clusters contain ONLY records (50453 record cells, 0
  ellipse/edge). Cluster label ALWAYS `<Role>_Session_<N>` (9594/9594). A cluster's
  records occupy a contiguous id range; clusters are ordered by increasing id
  (first-appearance == id order). ⇒ id allocation proceeds free-then-clusters in
  emission order; free ids < clustered ids.
- SHAPE census (all 12022 dot + all 14705 manifest dot bodies): shapes are ONLY
  record/ellipse/plain. NO trapezium/invtrapezium in captured output.
- Node-kind census over ellipse labels: besides `!KU(..)@#t` (79358), `#t:rule[..]`
  (100410), `Action(..)@#t` (3586), there are BARE TIMEPOINT ellipses `#<var>` —
  `#last` (107), `#i` (1328), `#decrypt` (790), `#t0..#t6`, `#j`, `#j.1`. `#last`
  is uncolored `n<ID>[label="#last",shape="ellipse"]`, the target of a
  `color="black",style="dashed"` before-edge (fixture 24a119958f784d43.dot).
- WRAP measurement (measure_break.py): physical line-0 visual width of 61821
  wrapped records ranges 19..277 (median 95) ⇒ NO fixed page width in absolute
  columns; the accumulated-column effect is real. Y_counter probe (4c6f68b4...):
  a 21-char fact `Y_counter( pid.1, t )` at line-start peels its `)` while an
  IDENTICAL-args longer instance late on a broken line does NOT ⇒ wrap trigger is
  Wadler `group`/`fits` (flat + rest-of-line vs remaining width), not per-cell-87.
- INFO action-list wrap style: one action PER LINE (vertical `sep`) even when short
  (6738eb64..: 4 actions, 4 lines; `Decrypt( m )` alone on its line), `]` attached
  to the last action. Contrast tuple/fact args = greedy fill (`fillSep`). b5a8773..
  info cell wraps at flat 52 (< 87) ⇒ info-cell trigger is also accumulated-column.

### Live server probes (scratchpad/serve.sh, --port= form; ports 3210–3213)
- wrap.spthy :3210 — 11 one-rule theories, each `rule EN/WP: [Fr(~s)] --[X()]->
  [Out(<..>)]` + exists-trace lemma. Autoproved each via GET autoprove/idfs/0/False
  /proof/l_X (returns {"redirect":"/thy/trace/<idx>/..."}), fetched
  interactive-graph-def/proof/l_X/_. Element sweep E10..E14 (5-col 'aNN' elems):
  11 elems fit one line (col 83 after trailing ", "), 'a12' wraps to col-6 indent;
  continuation packs GREEDILY (E13→2, E14→3 on the second line) to the SAME width —
  the earlier "+1 lookahead" is subsumed. Width sweep W69..W74 (`<'a…a'(P),'y'>`):
  P=71 flat 87 fits, P=72 flat 88 breaks. Peel: tuple `>` stays with last element
  iff it fits (W72), else peels to the `<` column on its own line (W74, indent 5);
  fact `)` ALWAYS peels to col 0 once the fact wraps (E12 peels `)` even though it
  would fit). Captured 7 fixtures (wrap_E11..E14, W71/W72/W74).
- ind.spthy :3211 — autoprove ord [use_induction]; the lemma discharged
  (redirect to `reach`) without surfacing a `#last` graph state in autoprove mode.
  `#last` node kind validated against corpus captured output instead (107 instances).
- decwrap-destr-manual.spthy (SAPIC) :3212 — autoprove can_create_key; captured a
  live cluster graph `cluster_Process_Session_1` (role="Process",
  node fillcolor="#ffffff", fontcolor="black", cluster ARGB "#36A5D84C"). Saved
  fixture cluster_process.dot. Confirms §4 cluster emission on a role theory live.
- NSLPK3.spthy :3213 — scanned interactive-graph-def/cases/{raw,refined}/i/j
  (i≤12,j≤8): invtrapezium PRESENT (n6 `(#i, 0)`, target of a conclusion port
  `n5:n2 -> n6` structural gray50 edge ⇒ invtrapezium = "conclusion feeding an
  absent node"). Plain `trapezium` (the dual: absent SOURCE feeding a present
  premise) ABSENT here and in every corpus/probe ⇒ recorded unobserved; the model's
  generic `GraphNode::Shaped{label,shape,color}` can emit it if a caller supplies one.
- All four servers stopped after probing (serve.sh stop 3210..3213).

### Implemented (graph-clean)
- render::wrap_cell — byte-exact wrap+peel: layout_fact (fill args / recurse into a
  sole tuple / peel `)` to col 0), layout_tuple (fill + `>` peel to `<`-col),
  layout_info (vertical action `sep`), run_layout (greedy fill), split_top_commas.
  Wired into generate::build_record for premise/info/conclusion cells.
- generate: cluster emission (RuleInstance/RawRule .cluster(label,color); free-then-
  cluster bucketing in emission/id order); GraphNode::Temporal (#var, incl. .last());
  GraphNode::Shaped (generic shape escape hatch, incl. trapezium dual); RawRule +
  GraphNode::RawRule (pre-rendered cell strings through the same wrap/escape pipeline).
- Tests: wrap_E12/E13/E14/W71/W72/W74 generation fixtures, cluster_process (live)
  + cluster_multi (corpus 79c) generation fixtures, last_timepoint (corpus 24a)
  fixture, RawRule==Term byte-parity + RawRule wrap, wrap_cell unit tests. Full
  GRAPHCLEAN_CORPUS round-trip stays 12022/12022 byte-exact.

## Session 6 — close the record-cell wrap TRIGGER (record-level rule)

### Corpus reads (captured OUTPUT: oracle/dot_corpus/ 12022)
- Re-parsed all 160409 records into groups/cells; reconstructed each cell's FLAT
  visual width from the wrapped DOT (dewrap: strip \l + &nbsp; runs, unescape
  \< \> to 1 col; add back 1 lost space per ",\l" fact/info break and per "\l)"
  paren peel — validated exactly on Ack(flat25)/Big(flat68) from ref_raw_1_1).
- Validated the derived rule vs actual \l over all 776259 cells: cell-level match
  99.635% (2831 mism), record-level 98.324% (157721/160409), info-cell 99.968%
  (52/160409). Mismatch T-distribution: 1459 at T<88, 1305 at T in [88,95], 15 at
  T>95 — ALL near the group-total boundary of 87 (see BEHAVIOR §3f).
- ref_raw_1_1.dot (prior agent, wide.spthy Wide rule) is the key fixture: In(prem,
  flat67) does NOT wrap while Big(concl, flat68) DOES — same tuple, opposite by
  group position; Ack(flat25) wraps, Out(h,flat14) does not. All four predicted
  exactly by budget_i = max(87 - Σ(other cell flats in same group), 20), wrap iff
  flat_i > budget_i.

### Live server probes (scratchpad/serve.sh --port= form; ports 3210-3221)
Autoprove flow: GET /thy/trace/N/autoprove/idfs/0/False/proof/<lemma> -> JSON
redirect to new theory index; GET interactive-graph-def/proof/<lemma>/_. drive.py
threads the latest index across lemmas. Distinct-atom / distinct-length constants
used to avoid abbreviation collisions (equal constants -> occ>=2 -> abbreviated,
which corrupted the first EQ/TQ sweeps).
- probe1 (:3211, 26 rules): premise width (PM) and rule-name width (R) do NOT change
  a lone conclusion's wrap (Out flat87 stayed flat with In flat 69/103/198 and with
  rule-name len 40) => GROUPS ARE INDEPENDENT (a conclusion's budget ignores
  premises/info). Adding ONE preceding conclusion (CC5, C flat12) made Out(flat87)
  wrap => same-group cells share the budget.
- probe2/probe3 (:3212/:3213, 60+120 rules): 2-cell conclusion group [Fa(flat p+8),
  Fb(flat q+8)]. Fixed-q sweeps of p pinned budget_Fa boundary EXACTLY: q=3(Fb11)
  -> Fa fits<=76 wraps>=77; q=20(28) -> <=58/>=61; q=40(48) -> <=39/>=40; q=60(68)
  -> <=19/>=22. In every case budget_Fa + Fb_flat = 87 => budget_Fa = 87 - Fb_flat.
- probe4 (:3214, 80 rules): step-1 boundaries confirm 87 (q40: Fa fit@flat39
  wrap@flat40, budget=87-48=39). FLOOR probe (Fb flat98, Fa small): Fa fits<=flat20
  wraps>=flat21 => budget floored at 20. NARROW probe (Fa flat98, Fb small):
  symmetric, Fb fit<=20 wrap>=21. 3-cell [Fa,Fb28,Fc28]: Fa fit@flat31 wrap@flat33,
  budget=87-56=31 => Σ is over ALL other cells in the group. Algebra: wrap iff
  flat_i > max(87-Σothers,20) <=> (group_total_flat > 87) AND (flat_i > 20).
- probe5/6/7/8 (:3215-3218): INFO cell. A single-action info wraps by width at
  flat>87 (budget 87, alone in its group; independent of wide prem/concl — probe8
  PW/CW/PC flat85 all fit). An info with >=2 action facts ALWAYS goes vertical (one
  action per line) regardless of width (TwoShort flat34, ThreeShort flat43, AB
  flat22 all wrapped). Corpus census: 0 non-wrapped info cells with a top-level
  action-list comma (all >=2-action infos wrap). info-rule: wrap iff (#actions>=2)
  OR (flat>87).
- probe9/probeA (:3219/:3220): split sweeps at fixed group-total. F100 (T=100, well
  over 87) wraps every cell with flat>20 (clean, matches rule). E88 (T=88, each cell
  exactly budget+1) OSCILLATES fit/wrap with atom parity — the ±1 HughesPJ `fits`
  rounding artifact. So the rule is exact once a cell is >~1 past its budget; at the
  exact boundary the outcome depends on the GPL pretty-printer's internal `fits`.
- probeB (:3221): internal fill width when a multi-cell cell wraps is NOT a clean
  function of the budget (Pad flat 34 and 49 both yield a 9-element first line for
  the same W tuple though budgets differ 53 vs 38) — same intractable pretty-printer.
  The wrapped-content byte layout of a multi-cell wrap is a residual.
- All servers stopped (serve.sh stop 3210..3221).

## Session 7 — refine the group wrap BUDGET (fill width) + decompose the residual

### Live server probes (scratchpad/serve.sh --port= form; ports 3200-3205)
Autoprove flow as Session 6. Distinct fill chars per fact (fix: equal-length atoms
across siblings collide -> occ>=2 -> ABBREVIATED, which corrupted the first
3-cell/equal-sibling sweeps; genlib.atom_fact takes a `fill` char now).
- batchA (:3200, 38 rules): reproduce the datum. 2-cell [Big(<30 5-char elems>),
  Sib(q)] both orders; read Big's line-0 element count M (budget in [7M+4,7M+10]).
  ORDER IRRELEVANT (AF==AS for every q). Fill budget FALLS SUB-LINEARLY with q and
  is FAR above the flat-sum 87-q: q=10 -> M=11 (budget 81-87, not 77); q=68 -> M=8
  (60-66). 3-cell wide-datum [big,25,14] -> M=7 (53-59), all 3 orders identical.
- batchB (:3201, 120 rules): fine PAD+'y' bisection of the fill budget. Alone -> 87
  exact. But the 2-element target gives a DIFFERENT budget than the 30-element one
  for the same sibling (q=10: batchA 81+, batchB <78) => the fill is fits-based
  (fillSep), NOT a single per-cell budget.
- batchC (:3202, 99 rules): single-atom target TRIGGER boundary (the ')' peel).
  budget = 87 - Sigma(other flats), floor 20, EXACT for 2-cell q=10..58 and 3-cell
  [25,14]->48, [10,10]->67. Deviation only when a sibling is FORCED to wrap at the
  target's boundary: [Ta,68]-> 21 (=87-occ(Hg)=87-66), not floor 20.
- batchD/E (:3203/:3204, 56+111 rules): CLEAN distinct fills. With no abbreviation
  contamination the earlier "+3" 3-cell deviations VANISH: [30,30]->27, [24,24]->39,
  [40,10]->37, [25,14]->48 all = flat-sum(floor). The lone occ effect [Ta,Hg68]->21
  is real (sibling occ = flat-2 = 66; D4 confirmed a wrapped single-atom occ = flat-2).
- batchG (:3205, 66 rules): ORDER test with COMPARABLE cells at the margin and with
  WRAPPING siblings. [A,B]==[B,A] boundary 47 (q40) / 27 (q60); [A,30,30] gives the
  same A boundary in all 3 positions. ORDER DEFINITIVELY IRRELEVANT.
- All servers stopped (serve.sh stop 3200..3205); ports 3200-3299 clear.

### Corpus analysis (captured OUTPUT: oracle/dot_corpus/ 12022)
- vc_r7 / vc_resid: flat-sum TRIGGER = 99.5488% of 615850 prem/concl cells (0.45%
  mismatch). Residual: false_pos 1320 (T 88-97; 1309 have a WRAPPING sibling ->
  occ-relief, a small cell fits beside a wide wrapping sibling), false_neg 1459
  (T 79-87). Naive occ-everywhere is WORSE (1.24%); guarded occ-relief also worse
  (0.82%) => flat-sum is the best closed-form trigger.
- vc_expand: 954/1459 false_neg (65%) explained by the cell's UNABBREVIATED width
  exceeding its budget (KD19/EN2 etc. expand past 87; abbreviations substituted into
  the already-broken layout). Rest ~505 are the +-1 fits boundary.
- vc_fill (Python port of render.rs fill): FILL byte-exactness of 142540 wrapping
  prem/concl cells under candidate budgets: flat-sum 41.45%, sibling-occ 43.29%,
  smallest-first greedy min(flat,budget) 44.11% (best). The last reproduces the live
  Wide record byte-exact (Ack@20, Big@56 -> 8 elements). Implemented in
  generate::group_cells; corpus round-trip stays 12022/12022; new test
  wide_conclusion_group_fill_byte_exact (fixture wide_record.dot vs live wide_group.dot).

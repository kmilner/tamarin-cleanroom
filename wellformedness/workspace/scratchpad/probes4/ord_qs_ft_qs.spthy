theory OrdQFQ
begin
rule R1:
   [ Fr(~x) ]
  --[ A(~x) ]->
   [ Out(~x) ]
lemma L1:
  all-traces
  "All $x #i. (A($x) @ #i) ==> (A($x) @ #i)"
lemma L2:
  exists-trace
  "Ex #i. A(y) @ #i"
lemma L3:
  all-traces
  "All ~z #i. (A(~z) @ #i) ==> (A(~z) @ #i)"
end

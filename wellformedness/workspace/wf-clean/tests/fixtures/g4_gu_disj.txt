WARNING: the following wellformedness checks failed!

 Formula guardedness
====================

  Lemma `L' cannot be converted to a guarded formula:
    universal quantifier without toplevel implication
      "∀ x #i. (A( x ) @ #i) ∨ (B( x ) @ #i)"
    in the formula
      "∀ x #i. (A( x ) @ #i) ∨ (B( x ) @ #i)"
*/

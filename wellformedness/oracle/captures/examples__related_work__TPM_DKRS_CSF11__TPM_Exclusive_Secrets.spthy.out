maude tool: 'maude'
 checking version: 3.5.1. OK.
 checking installation: OK.
[Theory TPM_Exclusive_Secrets] Theory loaded
[Theory TPM_Exclusive_Secrets] Theory translated
[Theory TPM_Exclusive_Secrets] Derivation checks started
[Theory TPM_Exclusive_Secrets] Derivation checks ended
[Theory TPM_Exclusive_Secrets] Theory closed
theory TPM_Exclusive_Secrets

begin

// Function signature and definition of the equational theory E

functions: adec/2, aenc/2, fst/1, h/1, pair/2, pk/1, sign/2, snd/1,
           true/0, verify/3
equations:
    adec(aenc(x.1, pk(x.2)), x.2) = x.1,
    fst(<x.1, x.2>) = x.1,
    snd(<x.1, x.2>) = x.2,
    verify(sign(x.1, x.2), x.1, pk(x.2)) = true

rule (modulo E) PCR_Init:
   [ Fr( ~aik ) ]
  --[ PCR_Init( 'pcr0', ~aik ), UniqueInit( ) ]->
   [ PCR( 'pcr0' ), !AIK( ~aik ), Out( pk(~aik) ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) PCR_Extend:
   [ PCR( x ), In( y ) ]
  --[ PCR_Extend( x, y, h(<x, y>) ) ]->
   [ PCR( h(<x, y>) ) ]

  // loop breaker: [0]
  /* has exactly the trivial AC variant */

rule (modulo E) PCR_CreateKey:
   [ Fr( ~ska ) ] --> [ !KeyTable( h(<'pcr0', $a>), ~ska ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) PCR_CertKey:
   [ !AIK( aik ), !KeyTable( x, sk ) ]
  --[ PCR_CertKey_Inst( x ) ]->
   [ Out( sign(<'certkey', x, pk(sk)>, aik) ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) PCR_Unbind:
   [ PCR( x ), !KeyTable( x, sk ), In( aenc(m, pk(sk)) ) ]
  --[ PCR_Unbind( x, sk, m ) ]->
   [ PCR( x ), Out( m ) ]

  // loop breaker: [0]
  /* has exactly the trivial AC variant */

rule (modulo E) Alice_Init:
   [
   Fr( ~s0 ), Fr( ~s1 ), !AIK( aik ), In( sign(<'certkey', x0, pk0>, aik) ),
   In( sign(<'certkey', x1, pk1>, aik) )
   ]
  --[ InEq( x0, x1 ), Secrets( ~s0, ~s1 ) ]->
   [ Out( aenc(~s0, pk0) ), Out( aenc(~s1, pk1) ) ]

  /* has exactly the trivial AC variant */

restriction UniqueInit_unique:
  "∀ #i #j. ((UniqueInit( ) @ #j) ∧ (UniqueInit( ) @ #i)) ⇒ (#i = #j)"
  // safety formula

  /*
  expanded formula:
  "∀ #i #j. ((UniqueInit( ) @ #j) ∧ (UniqueInit( ) @ #i)) ⇒ (#i = #j)"
  */

restriction Ineq_checks_succeed:
  "∀ t #e. (InEq( t, t ) @ #e) ⇒ (⊥)"
  // safety formula

  /*
  expanded formula:
  "∀ t #e. (InEq( t, t ) @ #e) ⇒ (⊥)"
  */

lemma types [sources]:
  all-traces
  "∀ m d1 d2 #i.
    (PCR_Unbind( d1, d2, m ) @ #i) ⇒
    (((∃ #j. (!KU( m ) @ #j) ∧ (#j < #i)) ∨ (∃ s #j. Secrets( m, s ) @ #j)) ∨
     (∃ s #j. Secrets( s, m ) @ #j))"
/*
guarded formula characterizing all counter-examples:
"∃ m d1 d2 #i.
  (PCR_Unbind( d1, d2, m ) @ #i)
 ∧
  (∀ #j. (!KU( m ) @ #j) ⇒ ¬(#j < #i)) ∧
  (∀ s #j. (Secrets( m, s ) @ #j) ⇒ ⊥) ∧
  (∀ s #j. (Secrets( s, m ) @ #j) ⇒ ⊥)"
*/
by sorry

lemma Unbind_PCR_charn [reuse, use_induction]:
  all-traces
  "∀ x sk m #i.
    (PCR_Unbind( x, sk, m ) @ #i) ⇒
    ((∃ aik #j. PCR_Init( x, aik ) @ #j) ∨
     (∃ y xPrev #j. PCR_Extend( xPrev, y, x ) @ #j))"
/*
guarded formula characterizing all counter-examples:
"∃ x sk m #i.
  (PCR_Unbind( x, sk, m ) @ #i)
 ∧
  (∀ aik #j. (PCR_Init( x, aik ) @ #j) ⇒ ⊥) ∧
  (∀ y xPrev #j. (PCR_Extend( xPrev, y, x ) @ #j) ⇒ ⊥)"
*/
by sorry

lemma exclusive_secrets:
  all-traces
  "¬(∃ s0 s1 #i #d0 #d1.
      ((Secrets( s0, s1 ) @ #i) ∧ (K( s0 ) @ #d0)) ∧ (K( s1 ) @ #d1))"
/*
guarded formula characterizing all counter-examples:
"∃ s0 s1 #i #d0 #d1.
  (Secrets( s0, s1 ) @ #i) ∧ (K( s0 ) @ #d0) ∧ (K( s1 ) @ #d1)"
*/
by sorry

lemma left_reachable:
  exists-trace "∃ s0 s1 #i #j. (Secrets( s0, s1 ) @ #i) ∧ (K( s0 ) @ #j)"
/*
guarded formula characterizing all satisfying traces:
"∃ s0 s1 #i #j. (Secrets( s0, s1 ) @ #i) ∧ (K( s0 ) @ #j)"
*/
by sorry

lemma right_reachable:
  exists-trace "∃ s0 s1 #i #j. (Secrets( s0, s1 ) @ #i) ∧ (K( s1 ) @ #j)"
/*
guarded formula characterizing all satisfying traces:
"∃ s0 s1 #i #j. (Secrets( s0, s1 ) @ #i) ∧ (K( s1 ) @ #j)"
*/
by sorry

/*
WARNING: the following wellformedness checks failed!

Message Derivation Checks
=========================

  The variables of the following rule(s) are not derivable from their premises, you may be performing unintended pattern matching.

Rule Alice_Init: 
Failed to derive Variable(s): pk0, pk1, x0, x1
*/

/*
Generated from:
Tamarin version 1.13.0
Maude version 3.5.1
Git revision: 0234f6a1abee25677c0accef62de0ac2883b0347 (with uncommited changes), branch: HEAD
Compiled at: 2026-07-16 18:33:16.036427196 UTC
*/

end

==============================================================================
summary of summaries:

analyzed: examples/related_work/TPM_DKRS_CSF11/TPM_Exclusive_Secrets.spthy

  processing time: 0.22s
  
  WARNING: 1 wellformedness check failed!
  
  types (all-traces): analysis incomplete (1 steps)
  Unbind_PCR_charn (all-traces): analysis incomplete (1 steps)
  exclusive_secrets (all-traces): analysis incomplete (1 steps)
  left_reachable (exists-trace): analysis incomplete (1 steps)
  right_reachable (exists-trace): analysis incomplete (1 steps)

==============================================================================
